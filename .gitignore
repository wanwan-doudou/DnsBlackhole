# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# 项目内缓存、界面审查记录与本地发布产物
.pnpm-store/
.impeccable/
artifacts/
.release-bench/
RELEASE_PROCESS.local.md
HANDOFF.local.md

# 机器本地的 cargo 配置（含本机 Windows SDK 路径，勿入库）
src-tauri/.cargo/
src-tauri/target/

# macOS 后台服务 sidecar 构建产物（由 scripts/build-macos-service.sh 生成）
src-tauri/binaries/

# 内部移植执行清单，只在本地维护，不进入公开仓库
MACOS_PORTING_PLAN.md

# 新版本规划只在本地维护，不进入公开仓库
新版本规划/v0.2.4.md

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 本地代码签名证书（私钥不入库，CI 从 GitHub Secrets 读取）
.signing/
.claude/
.playwright-mcp/
.agents/
